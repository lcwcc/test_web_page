# Metabolic Evidence Fieldnotes

A small Jekyll research journal published with GitHub Pages.

## Structure

- `_posts/`: Markdown fieldnotes
- `_layouts/`: site and article layouts
- `assets/style.css`: visual system
- `sitemap.xml` and `robots.txt`: discovery files

The public site is built from the `main` branch.

